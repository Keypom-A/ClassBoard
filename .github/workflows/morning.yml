name: MorningSchedule

on:
  schedule:
    - cron: "0 21 * * *"  # JST 6:00 = UTC 21:00

jobs:
  call_api:
    runs-on: ubuntu-latest
    steps:
      - name: Call ClassBot API
        run: curl https://classboard-1.onrender.com/api/bot/morning_schedule
